//surface area
function rectSA(length, width, height) {
    return (2 * length * height) + (2 * length * width) + (2 * width * height);
}


//volume
function rectVol(length, width, height) {
    return length * width * height;
};